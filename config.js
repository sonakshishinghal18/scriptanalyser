/**
 * Runtime configuration — edit this file to point the frontend at your backend.
 *
 * On Render: set the BACKEND URL here before deploying, or use the
 * environment variable approach described in README.md.
 *
 * Leave API_URL as empty string ('') when running locally with the
 * Render static server, since requests to /api/* will hit the same origin.
 *
 * When backend and frontend are on DIFFERENT Render services (which they are),
 * set API_URL to your backend's full URL, e.g.:
 *   window.API_URL = 'https://scriptforge-backend.onrender.com';
 */
window.API_URL = 'https://scriptanalyser.onrender.com';   // ← Replace with your backend URL before deploying
